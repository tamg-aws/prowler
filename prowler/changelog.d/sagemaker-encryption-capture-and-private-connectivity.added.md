Add four AWS SageMaker AI checks: `sagemaker_training_jobs_output_kms_encryption_enabled` for the KMS key on the model artifacts a training job writes to Amazon S3, `sagemaker_transform_job_volume_and_output_encryption_enabled` for the KMS keys on a batch transform job's compute volume and output, `sagemaker_endpoint_data_capture_enabled` for the inference data capture that SageMaker Model Monitor depends on, and `sagemaker_vpc_endpoints_configured` for private connectivity to the `sagemaker.api` and `sagemaker.runtime` APIs
